{
  "name": "titanx",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "TypeScript Agent SDK for TitanX",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "npm run build && node --test test/**/*.test.mjs"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "@libsql/client": "^0.14.0",
    "e2b": "^2.14.0",
    "hono": "^4.7.7",
    "pg": "^8.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@types/pg": "^8.11.10",
    "typescript": "^5.6.3"
  }
}
